# RustBPE

RustBPE is a custom Byte-Pair Encoding (BPE) implementation written in Rust for performance.

## Role
It is primarily used for **training** the tokenizer (learning the merge rules from a corpus). It is designed to be compatible with GPT-4's tokenization scheme.
